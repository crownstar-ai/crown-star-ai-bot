# Add to crownstar_ultra_cli.py:
# import requests, json
# def cmd_agent(self, args):
#     parts = args.split()
#     if not parts or parts[0] == "help":
#         print("Usage: /agent create <name> [type] [max_iter]          – create agent")
#         print("       /agent run <name> <task>                         – run agent")
#         print("       /agent stop <name>                               – stop agent")
#         print("       /agent list                                      – list agents")
#         print("       /agent tools                                     – list tools")
#         return
#     if parts[0] == "create" and len(parts) >= 2:
#         name = parts[1]
#         typ = parts[2] if len(parts) > 2 else "react"
#         max_iter = int(parts[3]) if len(parts) > 3 else 10
#         resp = requests.post("http://localhost:8080/v1/agent/create", json={"name": name, "type": typ, "max_iterations": max_iter})
#         print(resp.json())
#     elif parts[0] == "run" and len(parts) >= 3:
#         name = parts[1]
#         task = " ".join(parts[2:])
#         resp = requests.post(f"http://localhost:8080/v1/agent/{name}/run", json={"task": task})
#         data = resp.json()
#         print(f"Agent {name} result (duration {data.get('duration_seconds',0):.2f}s):")
#         print(data.get("result", ""))
#     elif parts[0] == "stop" and len(parts) >= 2:
#         name = parts[1]
#         resp = requests.get(f"http://localhost:8080/v1/agent/{name}/stop")
#         print(resp.json())
#     elif parts[0] == "list":
#         resp = requests.get("http://localhost:8080/v1/agent/list")
#         print(json.dumps(resp.json(), indent=2))
#     elif parts[0] == "tools":
#         resp = requests.get("http://localhost:8080/v1/agent/tools")
#         print(json.dumps(resp.json(), indent=2))
#     else:
#         print("Unknown agent command")
# Then register: "/agent": self.cmd_agent
