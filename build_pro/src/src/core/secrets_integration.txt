# In crownstar_core.py __init__, after loading config:
# from secrets.service import get_secrets
# self.secrets = get_secrets()
# # Resolve config secrets
# self.config = self.secrets.resolve_config(self.config)
# Also add a method to get API key for language models:
# def get_api_key(self, provider: str):
#     return self.secrets.get(f"api_keys/{provider}")
