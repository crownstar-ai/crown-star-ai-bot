[翻译占位] # Add to crownstar_enterprise_api.py or any API:
# from language_models.model_registry import ModelRegistry
# from language_models.model_router_enhanced import EnhancedModelRouter
#
# @app.get("/v1/models")
# async def list_models():
#     registry = ModelRegistry()
#     return {"models": registry.list_models_with_info(tier=core.tier)}
#
# @app.get("/v1/models/{model_name}")
# async def get_model_info(model_name: str):
#     registry = ModelRegistry()
#     info = registry.get_model(model_name)
#     if not info:
#         raise HTTPException(404, "Model not found")
#     return info

