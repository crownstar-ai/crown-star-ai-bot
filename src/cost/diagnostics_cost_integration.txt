# Extend cost optimizer to account for diagnostics runs (compute cost)
# In cost/optimizer/optimizer.py:
# def record_diagnostics_cost(self, duration_seconds):
#     self.metrics_queue.put(ResourceMetrics(
#         resource_id="diagnostics",
#         resource_type="compute",
#         provider="local",
#         region="cpu",
#         hourly_cost=0.05,  # compute cost
#         utilization_cpu=duration_seconds/3600,
#         utilization_memory=0.1,
#         utilization_disk=0,
#         timestamp=int(time.time())
#     ))
