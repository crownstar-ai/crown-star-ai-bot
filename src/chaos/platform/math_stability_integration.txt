# Extend chaos platform to inject numerical perturbations and verify Jacobian/Hessian
# Add to chaos/orchestrator/orchestrator.py:
# from diagnostics.model_diagnostics import get_diagnostics
# def test_mathematical_stability(self, perturbation_magnitude=0.01):
#     diag = get_diagnostics(self.model, self.device)
#     base_jac = diag.jacobian_report(dummy_input)
#     # Apply noise to weights
#     for p in self.model.parameters():
#         p.data += torch.randn_like(p) * perturbation_magnitude
#     perturbed_jac = diag.jacobian_report(dummy_input)
#     # Check if condition number degrades
#     degradation = perturbed_jac.condition_number / base_jac.condition_number
#     return {"degradation": degradation, "stable": degradation < 10.0}
