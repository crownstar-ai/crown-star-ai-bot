# Add to index.html – language dropdown and dynamic translation
# Add to settings panel:
# <label>Language: <select id="langSelect">
#   <option value="en">English</option>
#   <option value="zh">简体中文</option>
#   <option value="es">Español</option>
# </select></label>
#
# JavaScript function to change language via API:
# async function setLanguage(lang) {
#     await pywebview.api.set_language(lang);
#     location.reload(); // or re-render UI dynamically
# }
#
# Add to CrownStarAPI class:
# def set_language(self, lang: str) -> str:
#     from i18n.service import i18n
#     if i18n.set_language(lang):
#         return f"Language changed to {lang}"
#     return f"Unknown language: {lang}"
