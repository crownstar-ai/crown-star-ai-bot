[翻译占位] # Add to crownstar_ultra_cli.py:
# import requests
# import json
# def cmd_remediation(self, args):
#     parts = args.split()
#     if not parts or parts[0] == "help":
#         print("Usage: /remediation status")
#         print("       /remediation history [limit]")
#         print("       /remediation policies")
#         print("       /remediation run")
#         print("       /remediation trigger <policy_id> [--force]")
#         return
#     if parts[0] == "status":
#         resp = requests.get("http://localhost:8080/v1/remediation/status")
#         print(json.dumps(resp.json(), indent=2))
#     elif parts[0] == "history":
#         limit = int(parts[1]) if len(parts) > 1 else 20
#         resp = requests.get(f"http://localhost:8080/v1/remediation/history?limit={limit}")
#         data = resp.json()
#         for h in data["history"]:
#             status = "✅" if h["success"] else "❌"
#             print(f"{h['timestamp']} {status} {h['policy_id']} -> {h['action']}")
#     elif parts[0] == "policies":
#         resp = requests.get("http://localhost:8080/v1/remediation/policies")
#         data = resp.json()
#         for p in data["policies"]:
#             print(f"{p['id']} ({'enabled' if p['enabled'] else 'disabled'}): {p['condition']} -> {p['action']}")
#     elif parts[0] == "run":
#         resp = requests.post("http://localhost:8080/v1/remediation/run")
#         print(resp.json())
#     elif parts[0] == "trigger" and len(parts) >= 2:
#         policy_id = parts[1]
#         force = "--force" in args
#         resp = requests.post("http://localhost:8080/v1/remediation/trigger", json={"policy_id": policy_id, "force": force})
#         print(resp.json())
#     else:
#         print("Unknown remediation command")
#
# Then add "/remediation": self.cmd_remediation to commands dict.

