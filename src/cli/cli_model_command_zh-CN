[翻译占位] # Add to CrownStarCLI class in crownstar_cli.py:
#   def cmd_model(self, args):
#       if args:
#           try:
#               self.core.set_model(args)
#               print(f"Model switched to {args}")
#           except ValueError as e:
#               print(e)
#       else:
#           print("Available models:", self.core.model_router.list_models())

