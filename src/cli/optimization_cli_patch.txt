# Add to crownstar_ultra_cli.py:
# import numpy as np
# from optimization.sa.engine import SimulatedAnnealing, rosenbrock
# from optimization.qubo.solver import QUBOSolver, QUBOProblem, create_random_qubo
# from optimization.ga.engine import GeneticAlgorithm
# from optimization.core_integration import CrownStarOptimizer
# def cmd_optimize(self, args):
#     parts = args.split()
#     if not parts or parts[0] == "help":
#         print("Usage: /optimize sa|ga|qubo|prompt")
#         return
#     if parts[0] == "sa":
#         print("Running Simulated Annealing on Rosenbrock (2D)...")
#         bounds = np.array([[-5.0, 5.0]] * 2)
#         sa = SimulatedAnnealing(rosenbrock, bounds)
#         best, val = sa.optimize(verbose=True)
#         print(f"Best solution: {best}, value: {val}")
#     elif parts[0] == "ga":
#         print("Running Genetic Algorithm on sphere (2D)...")
#         from optimization.sa.engine import sphere
#         bounds = np.array([[-5.0, 5.0]] * 2)
#         ga = GeneticAlgorithm(sphere, bounds)
#         best, val = ga.optimize(verbose=True)
#         print(f"Best solution: {best}, value: {val}")
#     elif parts[0] == "qubo":
#         n = int(parts[1]) if len(parts) > 1 else 5
#         problem = create_random_qubo(n)
#         solver = QUBOSolver(problem)
#         x, val = solver.simulated_annealing()
#         print(f"QUBO (n={n}) solution: {x}, value: {val}")
#     elif parts[0] == "prompt":
#         opt = CrownStarOptimizer()
#         result = opt.optimize_prompt_parameters(method="sa")
#         print(f"Optimized prompt parameters: temperature={result['temperature']:.2f}, top_p={result['top_p']:.2f}, max_tokens={result['max_tokens']}")
#     else:
#         print("Unknown optimizer")
#
# Then add "/optimize": self.cmd_optimize to commands dict.
