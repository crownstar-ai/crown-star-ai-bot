[翻译占位] # Add to crownstar_ultra_cli.py:
# import requests
# import json
# def cmd_replication(self, args):
#     parts = args.split()
#     if not parts or parts[0] == "help":
#         print("Usage: /replication status")
#         print("       /replication sync")
#         print("       /failover trigger")
#         return
#     if parts[0] == "status":
#         resp = requests.get("http://localhost:8080/v1/replication/status")
#         print(json.dumps(resp.json(), indent=2))
#     elif parts[0] == "sync":
#         # Manual sync trigger (background)
#         print("Manual sync not implemented via CLI – use /replication status")
#     else:
#         print("Unknown replication command")
#
# def cmd_failover(self, args):
#     parts = args.split()
#     if parts and parts[0] == "trigger":
#         resp = requests.post("http://localhost:8080/v1/replication/failover/trigger")
#         print(resp.json())
#     else:
#         print("Usage: /failover trigger")
#
# Then add "/replication": self.cmd_replication, "/failover": self.cmd_failover to commands dict.

