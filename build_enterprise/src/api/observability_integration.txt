# In crownstar_enterprise_api.py, after creating app:
# from observability.tracing import setup_tracing, instrument_fastapi, instrument_requests
# from observability.profiling import ProfilingMiddleware
# from observability.debug_endpoints import router as debug_router
# 
# # Setup tracing
# setup_tracing(service_name="crownstar-api", jaeger_host=os.environ.get("JAEGER_HOST", "localhost"))
# instrument_fastapi(app)
# instrument_requests()
# 
# # Add profiling middleware (threshold 500ms)
# app.add_middleware(ProfilingMiddleware, threshold_ms=500)
# 
# # Add debug router
# app.include_router(debug_router)
