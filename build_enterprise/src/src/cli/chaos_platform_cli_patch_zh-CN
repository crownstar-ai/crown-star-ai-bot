[翻译占位] # Add to crownstar_ultra_cli.py:
# import requests
# import json
# def cmd_chaos_platform(self, args):
#     parts = args.split()
#     if not parts or parts[0] == "help":
#         print("Usage: /chaos platform run <provider> <scenario> <target_json> [duration]")
#         print("       /chaos platform experiments")
#         print("       /chaos platform status <exp_id>")
#         print("       /chaos platform stop <exp_id>")
#         print("       /chaos platform providers")
#         return
#     if parts[0] == "run" and len(parts) >= 4:
#         provider = parts[1]
#         scenario = parts[2]
#         target_json = parts[3]
#         duration = int(parts[4]) if len(parts) > 4 else 60
#         target = json.loads(target_json)
#         payload = {"provider": provider, "scenario_type": scenario, "target": target, "duration_seconds": duration}
#         resp = requests.post("http://localhost:8080/v1/chaos/platform/scenario/run", json=payload)
#         print(json.dumps(resp.json(), indent=2))
#     elif parts[0] == "experiments":
#         resp = requests.get("http://localhost:8080/v1/chaos/platform/experiments")
#         data = resp.json()
#         for e in data.get("experiments", []):
#             print(f"{e['id']}: {e['scenario_type']} on {e['provider']} – {e['status']}")
#     elif parts[0] == "status" and len(parts) >= 2:
#         exp_id = parts[1]
#         resp = requests.get(f"http://localhost:8080/v1/chaos/platform/experiments/{exp_id}")
#         print(json.dumps(resp.json(), indent=2))
#     elif parts[0] == "stop" and len(parts) >= 2:
#         exp_id = parts[1]
#         resp = requests.post(f"http://localhost:8080/v1/chaos/platform/experiments/{exp_id}/stop")
#         print(resp.json())
#     elif parts[0] == "providers":
#         resp = requests.get("http://localhost:8080/v1/chaos/platform/providers")
#         print(json.dumps(resp.json(), indent=2))
#     else:
#         print("Unknown chaos platform command")
#
# Then add "/chaos platform": self.cmd_chaos_platform to commands dict.

