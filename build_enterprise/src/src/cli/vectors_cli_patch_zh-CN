[翻译占位] # Add to crownstar_ultra_cli.py:
# import requests, json
# def cmd_vectors(self, args):
#     parts = args.split()
#     if not parts or parts[0] == "help":
#         print("Usage: /vectors create <name> [provider] [dimension] [metric]  – create index")
#         print("       /vectors list                                            – list indexes")
#         print("       /vectors ingest <index> <vectors_file>                   – ingest vectors")
#         print("       /vectors search <index> <vector_file> [top_k]            – dense search")
#         print("       /vectors hybrid <index> <query> <vector_file> [top_k]    – hybrid search")
#         print("       /vectors stats <index>                                   – index statistics")
#         return
#     if parts[0] == "create" and len(parts) >= 2:
#         name = parts[1]
#         provider = parts[2] if len(parts) > 2 else "faiss"
#         dim = int(parts[3]) if len(parts) > 3 else 384
#         metric = parts[4] if len(parts) > 4 else "cosine"
#         resp = requests.post("http://localhost:8080/v1/vectors/indexes/create", json={"name": name, "provider": provider, "dimension": dim, "metric": metric})
#         print(resp.json())
#     elif parts[0] == "list":
#         resp = requests.get("http://localhost:8080/v1/vectors/indexes")
#         print(json.dumps(resp.json(), indent=2))
#     elif parts[0] == "ingest" and len(parts) >= 3:
#         index = parts[1]
#         vec_file = parts[2]
#         with open(vec_file, 'r') as f:
#             docs = json.load(f)
#         for d in docs:
#             d["index_name"] = index
#         resp = requests.post("http://localhost:8080/v1/vectors/ingest", json={"documents": docs})
#         print(resp.json())
#     elif parts[0] == "search" and len(parts) >= 3:
#         index = parts[1]
#         vec_file = parts[2]
#         top_k = int(parts[3]) if len(parts) > 3 else 10
#         with open(vec_file, 'r') as f:
#             vector = json.load(f)
#         resp = requests.post("http://localhost:8080/v1/vectors/search", json={"index_name": index, "query_vector": vector, "top_k": top_k})
#         print(json.dumps(resp.json(), indent=2))
#     elif parts[0] == "hybrid" and len(parts) >= 4:
#         index = parts[1]
#         query_text = parts[2]
#         vec_file = parts[3]
#         top_k = int(parts[4]) if len(parts) > 4 else 10
#         with open(vec_file, 'r') as f:
#             vector = json.load(f)
#         resp = requests.post("http://localhost:8080/v1/vectors/hybrid_search", json={"index_name": index, "query": query_text, "query_vector": vector, "top_k": top_k})
#         print(json.dumps(resp.json(), indent=2))
#     elif parts[0] == "stats" and len(parts) >= 2:
#         index = parts[1]
#         resp = requests.get(f"http://localhost:8080/v1/vectors/stats/{index}")
#         print(json.dumps(resp.json(), indent=2))
#     else:
#         print("Unknown vectors command")
# Then register: "/vectors": self.cmd_vectors

