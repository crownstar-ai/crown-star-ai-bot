# Extend data mesh to support vector indexes as virtual tables
# In data_mesh/sources, add:
# {
#     "name": "vectors",
#     "type": "crownstar_vector",
#     "connection_params": {"index_name": "crownstar_main"},
#     "domain": "vectors",
#     "tags": ["search", "embeddings"]
# }
# Then implement SQL queries like: SELECT * FROM vectors WHERE vector_similarity(embedding) > 0.8
