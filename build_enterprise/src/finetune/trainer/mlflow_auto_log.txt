# Add to finetune/trainer/train_script.py, before training:
# from mlflow.service import get_mlflow_service
# mlflow_svc = get_mlflow_service()
# exp_id = mlflow_svc.get_or_create_experiment("crownstar_finetuning")
# mlflow_svc.start_run(run_name=f"job_{args.job_id}")
# mlflow_svc.log_params(vars(args))
# At end of training, log metrics and artifacts:
# mlflow_svc.log_metrics({"train_loss": loss})
# mlflow_svc.log_artifact(args.output_dir, artifact_path="model")
# mlflow_svc.end_run("FINISHED")
