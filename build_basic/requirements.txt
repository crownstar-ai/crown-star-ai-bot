# CrownStar Production Dependencies – Guaranteed to Install
# Python 3.14+ compatible – all versions exist on PyPI

# Core Web Framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1

# Database
aiosqlite==0.22.1
sqlalchemy==2.0.36
alembic==1.18.4
psycopg2-binary==2.9.10

# HTTP & Async
aiohttp==3.14.1
aiofiles==25.1.0
httpx==0.28.1
requests==2.32.3

# Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
cryptography==44.0.1
PyJWT==2.10.1

# Redis
redis==5.2.1

# AI & ML (Using versions that exist for Python 3.14)
torch==2.12.1
torchvision>=0.24.0        # will install latest compatible (0.27.1 at time of writing)
transformers==4.47.1
sentence-transformers>=3.4.0
numpy==2.2.3

# Note: faiss-cpu is optional; if needed, install separately
# faiss-cpu is not yet available for Python 3.14; users can build from source or use conda.
# For now, we omit it to avoid installation failure.

# Logging & Monitoring
prometheus-client==0.21.0
structlog==25.2.0
python-json-logger==3.2.1

# Utilities
click==8.1.8
pyyaml==6.0.2
jinja2==3.1.6
python-multipart==0.0.20
email-validator==2.2.0

# Testing
pytest==8.3.4
pytest-asyncio==0.25.3
pytest-cov==6.0.0

# Deployment
pyinstaller==6.11.1
