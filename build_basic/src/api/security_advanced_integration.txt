# In crownstar_enterprise_api.py, add:
# from security.advanced_middleware import OIDCMiddleware, mTLSMiddleware
# from security.oidc.api import router as oidc_router
# import json
# with open("config/security/oidc/providers.json") as f:
#     oidc_cfg = json.load(f)
# if oidc_cfg.get("enabled", False):
#     app.add_middleware(OIDCMiddleware)
# mtls_enabled = os.environ.get("MTLS_ENABLED", "false").lower() == "true"
# if mtls_enabled:
#     app.add_middleware(mTLSMiddleware, require_mtls=True)
# app.include_router(oidc_router)
