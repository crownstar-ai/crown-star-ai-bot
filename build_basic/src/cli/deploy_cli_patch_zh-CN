[翻译占位] # Add to crownstar_ultra_cli.py:
# import requests, json
# def cmd_deploy(self, args):
#     parts = args.split()
#     if not parts or parts[0] == "help":
#         print("Usage: /deploy create <name> <provider> <region>              – create environment")
#         print("       /deploy list                                           – list environments")
#         print("       /deploy run <env_id> <version> [strategy]              – deploy")
#         print("       /deploy status <deployment_id>                         – get status")
#         print("       /deploy failover <from_env> <to_env>                   – failover")
#         return
#     if parts[0] == "create" and len(parts) >= 4:
#         name, provider, region = parts[1], parts[2], parts[3]
#         resp = requests.post("http://localhost:8080/v1/deploy/environment", json={"name": name, "provider": provider, "region": region})
#         print(json.dumps(resp.json(), indent=2))
#     elif parts[0] == "list":
#         resp = requests.get("http://localhost:8080/v1/deploy/environments")
#         for env in resp.json().get("environments", []):
#             print(f"{env['env_id']}: {env['name']} ({env['provider']}/{env['region']})")
#     elif parts[0] == "run" and len(parts) >= 3:
#         env_id, version = parts[1], parts[2]
#         strategy = parts[3] if len(parts) > 3 else None
#         payload = {"environment_id": env_id, "version": version}
#         if strategy:
#             payload["strategy"] = strategy
#         resp = requests.post("http://localhost:8080/v1/deploy/deploy", json=payload)
#         print(json.dumps(resp.json(), indent=2))
#     elif parts[0] == "status" and len(parts) >= 2:
#         dep_id = parts[1]
#         resp = requests.get(f"http://localhost:8080/v1/deploy/deployments/{dep_id}")
#         print(json.dumps(resp.json(), indent=2))
#     elif parts[0] == "failover" and len(parts) >= 3:
#         from_env, to_env = parts[1], parts[2]
#         resp = requests.post("http://localhost:8080/v1/deploy/failover", json={"from_environment_id": from_env, "to_environment_id": to_env})
#         print(resp.json())
#     else:
#         print("Unknown deploy command")
# Then register: "/deploy": self.cmd_deploy

