# Add to crownstar_core.py – integrate vector memory
# Insert after `self.memory = CrownStarMemory()`:
#   from memory.vector_memory import CrownStarVectorMemory
#   self.vector_memory = CrownStarVectorMemory()
#
# In answer(), after storing conversation, also add to vector memory:
#   self.vector_memory.add_text(user_input, conv_id, timestamp)
#   self.vector_memory.add_text(final, conv_id, timestamp)
#
# Replace recall_similar with vector search:
#   def recall_similar(self, query, limit=5):
#       return self.vector_memory.search(query, limit)
