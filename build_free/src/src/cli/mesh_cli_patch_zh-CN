[翻译占位] # Add to crownstar_ultra_cli.py:
# import requests
# import json
# def cmd_mesh(self, args):
#     parts = args.split()
#     if not parts or parts[0] == "help":
#         print("Usage: /mesh status")
#         print("       /mesh traffic split <stable_weight> <canary_weight>")
#         print("       /mesh canary status")
#         print("       /mesh mtls enable")
#         return
#     if parts[0] == "status":
#         resp = requests.get("http://localhost:8080/v1/mesh/status")
#         print(json.dumps(resp.json(), indent=2))
#     elif parts[0] == "traffic" and len(parts) >= 3 and parts[1] == "split":
#         stable = int(parts[2])
#         canary = int(parts[3]) if len(parts) > 3 else 100 - stable
#         payload = {"weights": {"stable": stable, "canary": canary}}
#         resp = requests.post("http://localhost:8080/v1/mesh/traffic/split", json=payload)
#         print(resp.json())
#     elif parts[0] == "canary" and len(parts) >= 2 and parts[1] == "status":
#         resp = requests.get("http://localhost:8080/v1/mesh/canary/status")
#         print(json.dumps(resp.json(), indent=2))
#     elif parts[0] == "mtls" and len(parts) >= 2 and parts[1] == "enable":
#         resp = requests.post("http://localhost:8080/v1/mesh/mtls/enable")
#         print(resp.json())
#     else:
#         print("Unknown mesh command")
#
# Then add "/mesh": self.cmd_mesh to commands dict.

