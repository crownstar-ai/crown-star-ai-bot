[翻译占位] # Extend analytics collector to monitor federated learning metrics
# In analytics/core.py MetricsCollector._collect_crownstar_metrics add:
# from federated.server import get_fed_server
# server = get_fed_server()
# self.record(MetricPoint("federated.round", server.current_round, now, {}))
# if server.round_history:
#     last_round = server.round_history[-1]
#     self.record(MetricPoint("federated.num_clients", last_round["num_clients"], now, {}))
#     self.record(MetricPoint("federated.total_samples", last_round["total_samples"], now, {}))

