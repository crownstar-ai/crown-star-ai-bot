[翻译占位] # Add to crownstar_ultra_cli.py:
# from anomaly.detector import get_anomaly_detector
# from anomaly.remediation.orchestrator import get_healing_orchestrator
# from anomaly.monitoring_daemon import get_monitoring_daemon
# import json
# def cmd_anomaly(self, args):
#     parts = args.split()
#     if not parts or parts[0] == "help":
#         print("Usage: /anomaly status|history|heal <metric> <value>|daemon start|stop")
#         return
#     if parts[0] == "status":
#         detector = get_anomaly_detector()
#         print(f"Anomaly history count: {len(detector.anomaly_history)}")
#         print("Buffers:", {k: len(v) for k,v in detector.metrics_buffer.items()})
#     elif parts[0] == "history":
#         detector = get_anomaly_detector()
#         for a in detector.anomaly_history[-10:]:
#             print(f"{a['timestamp']} – {a['metric']}={a['value']:.2f} is_anomaly={a['is_anomaly']} score={a['score']:.3f}")
#     elif parts[0] == "heal" and len(parts) >= 3:
#         metric = parts[1]
#         value = float(parts[2])
#         anomaly = {"metric": metric, "value": value, "timestamp": time.time(), "is_anomaly": True}
#         healer = get_healing_orchestrator()
#         success = healer.handle_anomaly(anomaly)
#         print(f"Healing triggered: {'success' if success else 'failed'}")
#     elif parts[0] == "daemon" and len(parts) >= 2:
#         daemon = get_monitoring_daemon()
#         if parts[1] == "start":
#             daemon.start()
#             print("Monitoring daemon started")
#         elif parts[1] == "stop":
#             daemon.stop()
#             print("Monitoring daemon stopped")
#         else:
#             print("Unknown daemon command")
#     else:
#         print("Unknown command")
#
# Then add "/anomaly": self.cmd_anomaly to commands dict.

