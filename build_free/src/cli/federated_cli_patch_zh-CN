[翻译占位] # Add to crownstar_ultra_cli.py:
# import requests, json
# def cmd_federated(self, args):
#     parts = args.split()
#     if not parts or parts[0] == "help":
#         print("Usage: /federated register <client_id> <endpoint> <data_size>   – register client")
#         print("       /federated submit <client_id> <round> <weights_file>     – submit update")
#         print("       /federated aggregate                                     – aggregate updates")
#         print("       /federated clients                                       – list clients")
#         print("       /federated round start                                   – start new round")
#         print("       /federated rounds                                        – show history")
#         print("       /federated model                                         – get global model info")
#         return
#     if parts[0] == "register" and len(parts) >= 4:
#         client_id, endpoint, data_size = parts[1], parts[2], int(parts[3])
#         resp = requests.post("http://localhost:8080/v1/federated/register", json={"client_id": client_id, "endpoint": endpoint, "data_size": data_size})
#         print(resp.json())
#     elif parts[0] == "clients":
#         resp = requests.get("http://localhost:8080/v1/federated/clients")
#         print(json.dumps(resp.json(), indent=2))
#     elif parts[0] == "aggregate":
#         resp = requests.post("http://localhost:8080/v1/federated/aggregate")
#         print(json.dumps(resp.json(), indent=2))
#     elif parts[0] == "round" and len(parts) >= 2:
#         if parts[1] == "start":
#             resp = requests.post("http://localhost:8080/v1/federated/round/start")
#             print(resp.json())
#     elif parts[0] == "rounds":
#         resp = requests.get("http://localhost:8080/v1/federated/rounds")
#         print(json.dumps(resp.json(), indent=2))
#     elif parts[0] == "model":
#         resp = requests.get("http://localhost:8080/v1/federated/model")
#         print(f"Global model round: {resp.json().get('round')}")
#     else:
#         print("Unknown federated command")
# Then register: "/federated": self.cmd_federated

