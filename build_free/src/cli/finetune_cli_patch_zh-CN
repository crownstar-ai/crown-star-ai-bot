[翻译占位] # Add to crownstar_ultra_cli.py:
# import requests
# import json
# def cmd_finetune(self, args):
#     parts = args.split()
#     if not parts or parts[0] == "help":
#         print("Usage: /finetune start [--base_model <model>] [--dataset <path>] [--use-conversations]")
#         print("       /finetune list")
#         print("       /finetune status <job_id>")
#         print("       /finetune export")
#         print("       /finetune adapters")
#         return
#     if parts[0] == "start":
#         payload = {}
#         if "--use-conversations" in args:
#             payload["use_conversations"] = True
#         elif "--dataset" in args:
#             idx = args.index("--dataset")
#             payload["dataset_path"] = args[idx+1]
#         if "--base_model" in args:
#             idx = args.index("--base_model")
#             payload["base_model"] = args[idx+1]
#         resp = requests.post("http://localhost:8080/v1/finetune/train", json=payload)
#         print(resp.json())
#     elif parts[0] == "list":
#         resp = requests.get("http://localhost:8080/v1/finetune/jobs")
#         data = resp.json()
#         for j in data.get("jobs", []):
#             print(f"{j['job_id']}: {j['status']} – {j['base_model']}")
#     elif parts[0] == "status" and len(parts) >= 2:
#         job_id = parts[1]
#         resp = requests.get(f"http://localhost:8080/v1/finetune/jobs/{job_id}")
#         print(json.dumps(resp.json(), indent=2))
#     elif parts[0] == "export":
#         resp = requests.post("http://localhost:8080/v1/finetune/datasets/export")
#         print(resp.json())
#     elif parts[0] == "adapters":
#         resp = requests.get("http://localhost:8080/v1/finetune/adapters")
#         print(json.dumps(resp.json(), indent=2))
#     else:
#         print("Unknown finetune command")
#
# Then add "/finetune": self.cmd_finetune to commands dict.

